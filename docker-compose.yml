services:
  postgres:
    image: postgres:17
    ports:
      - "0:5432"
    environment:
      POSTGRES_USER: voiceapp
      POSTGRES_PASSWORD: voiceapp
      POSTGRES_DB: voiceapp
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
