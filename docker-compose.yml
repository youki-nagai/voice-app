services:
  postgres:
    image: postgres:17
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: voiceapp
      POSTGRES_PASSWORD: voiceapp
      POSTGRES_DB: voiceapp
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
